/*
 *  Application Config
 */

const config = {
  // eslint-disable-next-line no-undef
  env: process.env.REACT_APP_ENV || 'production',
  apiUrl: `https://furtogether.fly.dev`,
  // apiUrl: `http://localhost:8080`,
};

export default config;

/**
 *
 */
